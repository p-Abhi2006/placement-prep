package javacore.day06;

public class Scoping {
    public static void main(String[] args) {

    //Anything which is declared outside the scope
    //can be accessed within the scope
        // ie,. {} functions and loops

        //Anything which is declared inside the scope
        //ie,. {} functions and loops
        //cannot be accessed outside the scope
    //    redeclarations of variables inside the scope is not allowed
        //the variables which are declared inside the scope can be redeclared outside
} }
